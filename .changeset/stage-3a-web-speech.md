---
"@jrocha-io/tts": minor
---

Stage 3a: the WebSpeechEngine adapter (OS-native, zero-download fallback) implementing the TtsEngine port. Its Web Speech API surface is injected (SpeechApi), so it is fully unit-tested off-browser.
